const clonedeep = require( 'lodash.clonedeep' );

const c_lazy_image = require( '../../../../packages/larva-patterns/components/c-lazy-image/c-lazy-image.prototype' );
const paragraph = require( '../../../../packages/larva-patterns/modules/paragraph/paragraph.prototype' );

module.exports = {
	c_lazy_image,
	paragraph,
}