// Maybe there could be a test
const jsdom = require("jsdom");
const fs = require("fs");
const path = require("path");
const { JSDOM } = jsdom;

// const markup = fs.readFileSync( path.resolve( __dirname, 'a-truncate.html' ) ).toString();
// const dom = new JSDOM( markup );

// console.log( dom );